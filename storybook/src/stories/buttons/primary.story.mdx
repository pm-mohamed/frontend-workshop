import {Story, Meta, Canvas, Props, Preview, Description} from '@storybook/addon-docs';

<Meta title="Components/Buttons/Primary" />

export const Template = (args) => ({
    props: {
        text: { control: 'text' },
        type: {
            options: {
                Primary: 'button--primary',
                Secondary: 'button--secondary',
            },
            control: {
                type: 'radio',
            },
        },
    },
    template: `<button class="button" :class="[type]">
        {{text}}
    </button>`,
});

<Description markdown={`# Buttons`}/>

This contains definitions for all **Primary-Buttons**.

## All Primary Buttons Variations

<Canvas>
  <Story name="Primary" args={{type: 'button--primary', text: 'Primary Button'}}>
      { Template.bind({}) }
  </Story>
  <Story name="Primary - Disabled" args={{type: 'button--primary', text: 'Primary Button', disabled: true}}>
      { Template.bind({}) }
  </Story>
  <Story name="Primary - loading" args={{type: 'button--primary', text: 'Primary Loading', loading: true}}>
      { Template.bind({}) }
  </Story>
  <Story name="Primary - loading disabled" args={{type: 'button--primary', text: 'Primary Loading', disabled: true, loading: true}}>
      { Template.bind({}) }
  </Story>
</Canvas>
