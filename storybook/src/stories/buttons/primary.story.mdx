import {Story, Meta, Canvas, Props, Preview, Description} from '@storybook/addon-docs';

<Meta title="Components/Buttons/Primary" />

export const Template = (args) => ({
    props: {
        text: { control: 'text' },
        disabled: { control: 'boolean' },
        loading: { control: 'boolean' },
        type: {
            options: {
                Primary: 'button--primary',
                Secondary: 'button--secondary',
            },
            control: {
                type: 'radio',
            },
        },
    },
    template: `<button class="button" :class="[{'is-disabled': disabled, 'is-loading': loading}, type]"><span class="btn-text">{{text}}</span></button>`,
});

<Description markdown={`# Buttons`}/>

This contains definitions for all **Primary-Buttons**.

## All Primary Buttons Variations

<Canvas withToolbar isColumn>
  <Story name="Primary" args={{type: 'button--primary', text: 'Primary Button',disabled: false, loading: false}}>
      { Template.bind({}) }
  </Story>
  <Story name="Primary - Disabled" args={{type: 'button--primary', text: 'Primary Button Disabled', disabled: true, loading: false}}>
      { Template.bind({}) }
  </Story>
  <Story name="Primary - Loading" args={{type: 'button--primary', text: 'Primary Button Loading', disabled: false, loading: true}}>
      { Template.bind({}) }
  </Story>
  <Story name="Primary - Loading Disabled" args={{type: 'button--primary', text: 'Primary Button Disabled Loading', disabled: true, loading: true}}>
      { Template.bind({}) }
  </Story>
</Canvas>
